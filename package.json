{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build  --stream && cp -r src/frontend/packages/app/dist/ src/backend/client/",
    "start": "lerna run start  --stream"
  },
  "workspaces": {
    "packages": [
      "src/**"
    ]
  }
}
