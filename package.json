{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build  --stream && cp -r src/client/packages/app/dist/ src/backend/client/",
    "start": "lerna run start  --stream && cp -r src/client/packages/app/dist/ src/backend/client/"
  },
  "workspaces": {
    "packages": [
      "src/**"
    ]
  }
}
