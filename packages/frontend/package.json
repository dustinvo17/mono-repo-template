{
  "name": "frontend",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --parallel --stream",
    "start": "lerna run start --parallel --stream"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}